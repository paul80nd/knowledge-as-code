# A corpus that adopted no type

There is nothing under a type folder here, so the export carries no type at all. That is a valid
statement of what a corpus holds, and it is the state the bundle has to survive.
