# Working in Example Engineering

[`../../CLAUDE.md`](../../CLAUDE.md) at the repository root carries the conventions, the commands and the writing rules
for every corpus here. This page carries what is this corpus's alone.

* **Extend one fictional organisation**: Example Engineering, whose policies stay stack-agnostic and name no service.
  [`README.md`](README.md) explains why.
* **Pack after you change a record something downstream cites.** [`../payments/`](../payments/) and
  [`../dog-fooding/`](../dog-fooding/) read this corpus as a sealed package, so run `export` and then `pack` here before
  either of them restores.
