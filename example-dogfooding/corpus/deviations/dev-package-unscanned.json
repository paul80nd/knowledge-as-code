{
  "type": "deviations",
  "path": "deviations/package-unscanned.md",
  "trust": null,
  "fields": {
    "id": "dev-package-unscanned",
    "title": "Nothing scans or attests the package kac ships",
    "status": "active",
    "departs-from": [
      "eng:pol-TRUS.ATTEST",
      "eng:pol-TRUS.MALWARE"
    ],
    "owner": "human:paul.law",
    "accepted-on": "2026-09-07",
    "review-by": "2027-03-07",
    "closed-on": null,
    "applies-to": [
      "svc-kac"
    ],
    "tags": [
      "package",
      "provenance",
      "supply-chain"
    ]
  },
  "sections": {
    "What we are doing instead": "The package is built by the publish workflow from the commit on `main`, on a runner GitHub gives fresh for that run. Dependabot watches the dependencies, and [std-CONFIG] holds every pin to an exact version.\n\nWhoever installs `kac` gets a package whose provenance is the nuget.org listing and nothing else. There is no attestation naming the workflow, the commit or the runner that built it.",
    "Why we need it": "Both are available and neither is free. GitHub's artifact attestation needs the publish workflow reworked around it, and a scan needs a scanner chosen and a failure path decided. Neither has been done, and the package is a documentation tool built from a public repository.",
    "What compensates": "* The package is built by one workflow, whose run log is public and names the commit it built.\n* ctl-0007 stands a corpus up from the packed tool on every pull request, so what ships has been run.\n* Trusted publishing means no key exists that would let anybody else push under this name.\n* nuget.org refuses a second push to a version it already holds, so what shipped cannot be replaced.",
    "How it closes": "The publish workflow generates a build provenance attestation, and the release notes say how to verify it. A scan runs over the package before the push and fails the job on a finding. This record closes when both are in the workflow.",
    "Scope": "The `kac` package on nuget.org, and the plugin served from the marketplace branch."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/0c4894d30a8b492c6096209e667a2d0fd1e50d14/examples/dog-fooding/deviations/package-unscanned.md"
  }
}
