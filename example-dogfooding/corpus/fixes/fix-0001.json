{
  "type": "fixes",
  "path": "fixes/pip-cannot-install-yamllint.md",
  "trust": "human-reviewed",
  "fields": {
    "id": "fix-0001",
    "title": "pip cannot find the yamllint version the lint job pins",
    "status": "active",
    "symptom-keywords": [
      "actionlint",
      "brew",
      "homebrew",
      "linters",
      "macos",
      "no matching distribution found",
      "pip",
      "python",
      "requirements.txt",
      "yamllint"
    ],
    "applies-to": null,
    "verified": [
      {
        "at": "2026-09-02T17:27:31Z",
        "by": "human:paul.law"
      }
    ],
    "review-by": "2027-03-09",
    "tags": [
      "linting",
      "macos",
      "setup"
    ]
  },
  "sections": {
    "Symptom": "`pip install -r .github/requirements.txt` fails on a Mac, and the release it says is missing is the one the file pins.\n\n```text\nERROR: Could not find a version that satisfies the requirement yamllint==1.38.0 (from versions: 0.1.0, ... 1.37.1)\nERROR: No matching distribution found for yamllint==1.38.0\n```\n\nThe versions it offers stop one release short of the pin.",
    "Cause": "macOS ships `/usr/bin/python3` at 3.9, and yamllint 1.38.0 needs Python 3.10 or newer. pip filters the index down to the releases the running interpreter supports, so the pinned release is on PyPI and invisible from here. The error quotes the pin it was given, and says nothing about the interpreter.",
    "Resolution": "1. Install both linters from Homebrew, which installs a Python of its own.\n\n```sh\n   brew install yamllint actionlint\n   ```\n\n2. Check each version against what the `lint` job pins.\n\n```sh\n   yamllint --version    # 1.38.0\n   actionlint --version  # 1.7.12\n   ```\n\nHomebrew installs the pinned yamllint and the pinned actionlint together. The `lint` job installs actionlint with a\n`go install` line, so `.github/requirements.txt` does not list it.",
    "Why it happens": "The `lint` job installs Python 3.13 before it installs the requirements, so CI never sees this failure.\n`.github/requirements.txt` records what that job installs, not what a laptop can run. Dependabot moves the pin, so the\nminimum Python version rises again whenever yamllint drops an old one."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/8f512ea3ff81d7d312d028619e0c69708b57d9bd/examples/dog-fooding/fixes/pip-cannot-install-yamllint.md"
  }
}
