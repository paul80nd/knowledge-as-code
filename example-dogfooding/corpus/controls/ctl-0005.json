{
  "type": "controls",
  "path": "controls/0005-update-check.md",
  "trust": null,
  "fields": {
    "id": "ctl-0005",
    "title": "Every corpus holds the file the template sends it",
    "status": "active",
    "verifies": [
      "std-CONFIG"
    ],
    "mechanism": "ci",
    "frequency": "per-pr",
    "last-verified": null,
    "evidence": "The `corpora` job's log on the pull request, under the step \"Check the corpus is in step with the template\".",
    "applies-to": [
      "all"
    ],
    "review-by": "2027-09-22",
    "tags": [
      "overlay",
      "template"
    ]
  },
  "sections": {
    "What it checks": "* [std-CONFIG.a-value-living-in-more-than-one-tree-is-copied-and-proved] says you \"**MUST** run\n  `kac update --check --from ../../` in every corpus after that copy\".",
    "How it works": "The `corpora` job runs `dotnet run --project ../../tooling/kac -- update --check --from ../../` in each of the four corpora under `examples/`. `manifest.yaml` at the root lists the files the check covers, and each of its rules states which layer those files belong to.\n\nThe check reports two faults: a copy whose content differs from the template's, and a file in a corpus that the template sends nothing to. Either one exits non-zero.",
    "Coverage and gaps": "The matrix covers `examples/`. These files are authored in `template/`, and nothing compares `template/` against a copy of itself.\n\n`manifest.yaml` covers `.schema/` and `template/.plugin/`, and neither reaches a corpus here as a copy. `.schema/`\nlands on the path it was read from. Every corpus names `plugin.from`, so it reads the plugin tree from `template/`. A\ncorpus created elsewhere gets its own `.editorconfig`, and each corpus here declares that file under `skip:`."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/a3e51dfdbdf9966f57e5e5061dd36bd84e9217f6/examples/dog-fooding/controls/0005-update-check.md"
  }
}
