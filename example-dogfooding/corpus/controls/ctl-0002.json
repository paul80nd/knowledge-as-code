{
  "type": "controls",
  "path": "controls/0002-workflow-gate-tests.md",
  "trust": null,
  "fields": {
    "id": "ctl-0002",
    "title": "Every job in kac.yml sits behind the gate",
    "status": "active",
    "verifies": [
      "std-CI"
    ],
    "mechanism": "ci",
    "frequency": "per-pr",
    "last-verified": null,
    "evidence": "The `tool` job's log on the pull request, under the step \"Run kac.core unit tests\".",
    "applies-to": [
      "all"
    ],
    "review-by": "2027-09-22",
    "tags": [
      "continuous-integration",
      "github-actions"
    ]
  },
  "sections": {
    "What it checks": "* [std-CI.the-gate-runs-on-every-pull-request-into-main] says `kac.yml` \"**MUST** name every one of its jobs in the\n  `needs:` of its `validate` job\".",
    "How it works": "`WorkflowGateTests` in `tooling/kac.tests` reads `.github/workflows/kac.yml` and collects the keys under `jobs:`. It\ncompares that set against `validate`'s `needs:` plus `validate` itself. The `tool` job runs it under\n`dotnet test tooling/kac.tests`.\n\nComparing sets catches two faults. A job added with no line in `needs:` fails. A `needs:` entry listing a job the file no longer declares fails as well.",
    "Coverage and gaps": "The test reads `.github/workflows/kac.yml` and no other workflow. The publishing workflows gate nothing, so a job outside `validate`'s `needs:` is a fault only `kac.yml` can have.\n\nThe branch rule sits outside the repository. [ctl-0001] covers whether it still names `validate`."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/7fa40f335ebe4de49fd691fd66941c5047b5bb57/examples/dog-fooding/controls/0002-workflow-gate-tests.md"
  }
}
