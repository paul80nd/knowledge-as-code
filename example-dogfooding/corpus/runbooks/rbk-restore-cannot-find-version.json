{
  "type": "runbooks",
  "path": "runbooks/restore-cannot-find-version.md",
  "trust": null,
  "fields": {
    "id": "rbk-restore-cannot-find-version",
    "title": "kac restore cannot find the version the lock names",
    "status": "active",
    "severity": "sev3",
    "applies-to": null,
    "last-rehearsed": "never",
    "rehearsal-frequency": "on-change",
    "requires-tools": [
      "A .NET 10 SDK",
      "A checkout of this repository"
    ],
    "requires-access": [
      "Write access to the branch the fix lands on"
    ],
    "tags": [
      "imports",
      "restore",
      "versioning"
    ]
  },
  "sections": {
    "Symptoms": "* `kac restore` prints `restore: ../engineering/.dist/package holds no package for example-engineering <version>.` and\n  exits 1.\n* `kac validate` fails in `examples/payments` or `examples/dog-fooding`, and says to run `kac restore` first.\n* The `corpora` job in `kac.yml` fails at **Restore what this corpus consumes**, for those two corpora only. `library`\n  and `engineering` consume nothing, so they pass.\n\nNothing failed in the producer. `examples/engineering` validates, exports and packs.",
    "Impact": "Nothing in production. `examples/payments` and `examples/dog-fooding` cannot validate, so the `corpora` job fails and nothing merges."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/a3e51dfdbdf9966f57e5e5061dd36bd84e9217f6/examples/dog-fooding/runbooks/restore-cannot-find-version.md"
  }
}
