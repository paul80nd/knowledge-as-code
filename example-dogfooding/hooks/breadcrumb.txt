example-dogfooding 0.23.0 travels with this session as data, exported 2026-09-13.
controls. 10 records: A merge waits for the validate job, Every job in kac.yml sits behind the gate, Every workflow passes actionlint, Every YAML file passes yamllint, Every corpus holds the file the template sends it and 5 more.
deviations. 18 records: Branch size and a red main are habits here, The reasoning behind a decision lives in the commit that made it, No standard here says how a deviation is handled, A consumer of the export gets no notice period, No standard governs what kac generate computes and 13 more.
fixes. 2 records: pip cannot find the yamllint version the lint job pins and An .editorconfig key edited from a shell changes nothing in the IDE.
processes. 7 records: Add a record to a corpus, Add a knowledge type, Change the schema, Change the tool, Open a pull request and 2 more.
reports. 1 record: Clause coverage.
reports (from eng). 1 record: Framework coverage.
runbooks. 3 records: The golden suite reports a diff nobody meant, nuget.org answers 404 for a version it has already accepted and kac restore cannot find the version the lock names.
services. 4 records: Corpus package feed, Documentation site, kac and Plugin marketplace.
standards. 36 entries across 6 records: The documentation site and kac are usable without sight, a mouse or colour, Every check and every publish runs from a workflow in this repository, Configuration is committed, pinned and explained, The plugin carries the corpus, and a skill answers from what travelled, Prose reads the same way whoever wrote it and A version stamp says what it versions, and a move says what changed.
standards (from eng). 37 entries across 12 records: An interface is described by a contract in the repository, A container image is pinned by digest and runs as a non-root user, C# follows the runtime team's conventions, and the compiler enforces them, One artefact is built once and promoted to production, A dependency is pinned, screened and named in the inventory and 7 more.
tools. 13 records: actionlint, Claude Code CLI, .NET SDK, GitHub CLI, Markdig and 8 more.
adrs (from eng). 1 record: Knowledge as code: a structured, validated wiki that AI sessions can read and contribute to.
glossary (from eng). 33 entries across 2 records: Engineering and Knowledge as code.
policies (from eng). 243 entries across 23 records: Software we build is usable by everyone, Access is by individual identity, on least privilege, Agents propose, people decide, Every change is verified automatically, and failures block, Cost is a non-functional requirement and 18 more.
Ask the glossary-lookup skill before you answer from what you already know.
