descriptor-version: 1
corpus: test-consumer
content-version: "2.1.0"
role: consumer
upstream:
  url:
  template-version: 3
skip:
  - path: tooling/accepted-active.txt
    reason: Deliberately kept different in this corpus (test).
  - path: tooling/accepted-resolved.txt
    reason: Was different once; identical again now — the check should flag it resolved.
