---
id: svc-catalogue
type: service
tier: descriptive
status: live
repo: catalogue
platform: azure-function
criticality: critical
depends-on:
data-stores:
owner: human:alex.doe
---

# Catalogue

`Service: svc-catalogue` `LIVE`

## What it does

Carries a `platform` this corpus never stated, so `enum` reports it against the two values
the descriptor holds rather than against a range the schema declares.

## Where it lives

The `catalogue` repository.

## Environments

| Environment | URL                           |
|-------------|-------------------------------|
| Production  | https://catalogue.example.com |
