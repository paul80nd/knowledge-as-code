# Frameworks

The external frameworks this fixture's policies refer to. A policy's `Alignment` cells resolve here, so
this page is what stops those references failing `link-resolves`.

## Inspiration

### ISO 27001

Named so that `[ISO 27001:2022]` has somewhere to land. What our standing against a framework is belongs
on a page like this one and never in a policy.
