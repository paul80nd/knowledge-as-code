{
  "type": "services",
  "path": "services/thumbnailer.md",
  "trust": null,
  "fields": {
    "id": "svc-thumbnailer",
    "title": "Thumbnailer",
    "status": "live",
    "component-type": "api",
    "platform": "dotnet",
    "criticality": "critical",
    "repos": [
      "thumbnailer"
    ],
    "depends-on": null,
    "data-stores": null,
    "nfrs": null,
    "facets": null,
    "tags": [
      "internal"
    ]
  },
  "sections": {
    "What it does": "Reads a jacket image from blob storage and returns it at the dimensions and quality the request asks for. It stores no state and caches nothing. The edge does the caching.\n\n**Every request arrives through the edge.** [svc-covers-cdn] forwards to this service as its origin, and the edge caches the resized result. Nothing calls this service directly.",
    "Where it lives": "* **Repository**: [`thumbnailer`](https://git.example.com/example-libraries/thumbnailer)\n* **Platform**: ASP.NET Core (.NET 10)\n* **Deployed as**: App Service `app-thumbnailer-<env>`",
    "Dependencies": "None. It reads blob storage and returns an image.",
    "Data": "Reads the `covers` container in the shared storage account, configured as `ConnectionStrings__CoverStorage`. It owns nothing in that container: the nightly import pipeline and branch staff uploads fill it, and [svc-covers-cdn] records both."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/df3f2c874ea9d726dcafc9008d79a0ccc2fb2ab5/examples/library/services/thumbnailer.md"
  }
}
