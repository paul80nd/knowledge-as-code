{
  "type": "services",
  "path": "services/covers-cdn.md",
  "trust": null,
  "fields": {
    "id": "svc-covers-cdn",
    "title": "Covers CDN",
    "status": "live",
    "component-type": "asset",
    "platform": "static",
    "criticality": "critical",
    "repos": [
      "covers-import",
      "infrastructure"
    ],
    "depends-on": [
      "svc-thumbnailer"
    ],
    "data-stores": null,
    "nfrs": null,
    "facets": [
      "public"
    ],
    "tags": [
      "jackets"
    ]
  },
  "sections": {
    "What it does": "Serves every jacket image that appears in a result list, on an item page or on a branch terminal. Each one is resized and re-encoded to the dimensions the request asks for. The `?width=320&quality=80` form appears throughout the estate.\n\n**[svc-thumbnailer] is the origin, and blob storage is behind that service.** The thumbnailer reads the underlying container and resizes the image, so the edge caches the resized result. This service therefore has an endpoint of its own, separate from the estate's static one.\n\nContent arrives in the underlying container from two places:\n\n* `covers-import` brings jacket imagery from the bibliographic data supplier. Its own pipeline publishes it nightly.\n* [svc-catalogue-api] writes jackets uploaded by branch staff at runtime, for items the supplier has no image for.\n\n**`repos` names `covers-import` and `infrastructure`.** The infrastructure repository defines the edge, so a change to the edge is made there. `covers-import` publishes the jacket imagery, so a change to what the edge serves is made there. [svc-catalogue-api] also writes jackets, at runtime, which is a call and not a repository.",
    "Where it lives": "* **Repositories**: [`covers-import`](https://git.example.com/example-libraries/covers-import) publishes the imagery,\n  and [`infrastructure`](https://git.example.com/example-libraries/infrastructure) defines the edge at `services/covers`\n* **Platform**: CDN custom domain over an origin application\n* **Deployed as**: route `covers` on a dedicated endpoint, origin group `thumbnailer`, origin path `/covers`\n\nDNS is managed outside the infrastructure repository, as it is for the estate's other edge surfaces.",
    "Dependencies": "* [svc-thumbnailer] is the origin the edge forwards to. The CDN origin host sets it, so this dependency is recorded in\n  the routing and in neither service's application settings.",
    "Data": "Backed by the `covers` container in the shared storage account. The thumbnailer reads it on the edge's behalf."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/5f79f82cda118de12e19fea2feea18b495c47671/examples/library/services/covers-cdn.md"
  }
}
