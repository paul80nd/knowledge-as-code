{
  "type": "services",
  "path": "services/shelf-audit.md",
  "fields": {
    "id": "svc-shelf-audit",
    "title": "Shelf Audit",
    "status": "deprecated",
    "platform": "mixed",
    "criticality": "supporting",
    "repo": "shelf-audit",
    "depends-on": null,
    "data-stores": null,
    "facets": [
      "internal",
      "scheduled"
    ],
    "tags": [
      "monitoring"
    ]
  },
  "sections": {
    "What it does": "Once a night it walks the list of branch catalogue URLs and checks each one. The check confirms that both the bare and\n`www` forms respond over HTTPS. It collates the results and emails them.\n\nIt is the estate's only outside-in check that the catalogue is reachable. Nothing consumes its output except a mailbox. Whether an unread nightly email is the right home for that signal is the question behind its deprecation.",
    "Where it lives": "* **Repository**: [`shelf-audit`](https://git.example.com/example-libraries/shelf-audit)\n* **Platform**: a PowerShell function and a workflow app, deployed together\n* **Deployed as**: both, by Terraform from within the same repository\n\n**It is deployed unlike anything else in the estate.** There is no release pipeline. The repository holds its own Terraform configuration, which somebody applies from a workstation against a dedicated workspace. It runs in a pay-as-you-go subscription, while the rest of the estate uses a subscription per environment.\n\n`platform` is `mixed`: a PowerShell function and a workflow app are two runtimes. Terraform deploys this service, and\ndeployment does not decide the field.",
    "Dependencies": "None. It reads the branch list from a file in its own repository and checks the catalogue from the outside, as any reader would."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/a749e0a98a03707f5166755d6d727718c8f6b6e9/examples/library/services/shelf-audit.md"
  }
}
