{
  "type": "services",
  "path": "services/reservations.md",
  "trust": null,
  "fields": {
    "id": "svc-reservations",
    "title": "Reservations",
    "status": "building",
    "component-type": "website",
    "platform": "dotnet",
    "criticality": "important",
    "repos": [
      "platform"
    ],
    "depends-on": [
      "svc-lending"
    ],
    "data-stores": null,
    "nfrs": null,
    "facets": [
      "public"
    ],
    "tags": [
      "holds",
      "payments"
    ]
  },
  "sections": {
    "What it does": "Hosts the reserve-and-pay flow as a server-rendered application. It deploys on its own, apart from the rest of the monorepo, and is served on its own hostname. The flow is multistep and progressive, so it works on the branch terminals as well as on a reader's own device.\n\n**Stateless.** Wizard state before confirmation is ephemeral and passed through the flow. The only durable write is the provisional hold it creates in [svc-lending] at confirm. That hold is also the reference a returning card payment is matched against. Resuming a half-finished reservation in a later session is deliberately not supported.",
    "Where it lives": "* **Repository**: [`platform`](https://git.example.com/example-libraries/platform), at `src/Services/Reservations`\n* **Platform**: ASP.NET Core Razor Pages (.NET 10)\n* **Deployed as**: App Service `app-reservations-<env>`\n\nThe `platform` release pipeline deploys it as the `reservations` app, from the `Reservations.Web.zip` package. It is the third deployable from that repository.",
    "Dependencies": "* [svc-lending] is configured as `Apis__Lending__Url`. This service calls it over the private network.",
    "Data": "**None of its own** (verified): the infrastructure declares no database, no storage account and no connection string for this app service. The single durable write is the provisional hold created in [svc-lending], which owns it."
  },
  "links": {
    "human": "https://github.com/paul80nd/knowledge-as-code/blob/e48c7afc845b51e94ff96993bd5217b259efdc45/examples/library/services/reservations.md"
  }
}
